<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema de inicio para Android 12+ (API 31+) -->
    <!-- Forzamos el icono a transparente para que NO salga el logo estático del sistema -->
    <style name="Theme.App.Starting" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowSplashScreenAnimatedIcon">@android:color/transparent</item>
        <item name="android:windowSplashScreenBackground">#F8FAFC</item>
    </style>
</resources>
