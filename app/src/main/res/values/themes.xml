<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema Principal de la Aplicación (Premium SaaS Senior) -->
    <style name="Theme.RAYMI" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">#4F46E5</item> <!-- Indigo Primary -->
        <item name="android:windowBackground">#F8FAFC</item> <!-- Slate 50 (Fondo limpio) -->
        <item name="android:statusBarColor">#4F46E5</item> <!-- Barra de estado Indigo -->
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Tema de Inicio Ultra-Rápido: Invisible hasta que Compose arranca -->
    <style name="Theme.App.Starting" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
