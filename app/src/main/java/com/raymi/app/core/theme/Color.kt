package com.raymi.app.core.theme

import androidx.compose.ui.graphics.Color

// Colores inspirados en la cultura Inca y textiles peruanos
object RaymiColors {
    // Dorado Inca (Color principal)
    val Gold = Color(0xFFFFD700)
    val GoldVariant = Color(0xFFFFB300)
    val GoldLight = Color(0xFFFFE57F)

    // Rojo Inca (Acento)
    val IncaRed = Color(0xFFD62828)
    val IncaRedDark = Color(0xFFA4161A)
    val IncaRedLight = Color(0xFFE76F51)

    // Naranja terracota
    val Terracota = Color(0xFFF77F00)
    val TerracotaLight = Color(0xFFFCAE1E)

    // Azul profundo (Cielo andino)
    val DeepBlue = Color(0xFF003049)
    val DeepBlueLight = Color(0xFF004E6E)

    // Morado (Textiles tradicionales)
    val RoyalPurple = Color(0xFF6A0572)
    val PurpleLight = Color(0xFF9D4EDD)

    // Tonos oscuros (Dark theme)
    val DarkBackground = Color(0xFF0F0F0F)
    val DarkSurface = Color(0xFF1E1E1E)
    val DarkCard = Color(0xFF2A2A2A)

    // Tonos claros
    val LightBackground = Color(0xFFFFF8E1)
    val LightSurface = Color(0xFFFFFBF0)
    val LightCard = Color(0xFFFFFFFF)

    // Estados
    val Success = Color(0xFF4CAF50)
    val Warning = Color(0xFFFFC107)
    val Error = Color(0xFFF44336)
    val Info = Color(0xFF2196F3)

    // Textos
    val TextPrimary = Color(0xFFFFFFFF)
    val TextSecondary = Color(0xFFB0B0B0)
    val TextTertiary = Color(0xFF808080)

    val TextPrimaryLight = Color(0xFF1C1B1F)
    val TextSecondaryLight = Color(0xFF5A5A5A)
}